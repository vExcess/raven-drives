<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="static/styles.css">
</head>
<body>
    <div class="navbar">
        <a href="" class="brand">RAVEN<span>DRIVES</span></a>
        <div>
            <% if (userData?.displayName) { %>
                <span class="display_name" id="user_display_name"><%= userData.displayName %></span>
            <% } else if (!url.startsWith("login")) { %>
                <a href="login" class="btn" style="padding: 8px 16px; font-size: 0.8rem;">Login</a>
            <% } %>
        </div>

        <div>
            <% if(userData?.displayName){ %>
                <div class = "dropdown">
                    <button class="dropbtn">               
                        <span class="display_name" id="user_display_name"><%= userData.displayName %></span>
                    </button>
                    <div class="dropdown-content">
                        <a href="account">Account</a>
                        <a href="rides">Rides</a>
                        <a href="offers">Offers</a>
                        <a href="requests">Requests</a>
                        <a href="help">Help</a> 
                    </div>
                </div>  
            <% } else { %>
                <div class = "dropdown">
                    <button class  = "dropbtn">
                        <a href="login" class="btn" style="padding: 8px 16px; font-size: 0.8rem;">Dashboard</a>
                    </button>
                    <div class ="dropdown-content">
                        <a href = "login"> Login</a>
                        <a href = "createaccount">Create an Account</a> 
                    </div>
            </div>
            <% } %> 
            
        </div>

    </div>

    <div class="container">
        <%- content %>
    </div>

</body>
</html>
