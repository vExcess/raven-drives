<div class="centered-form-wrapper" style="width: fit-content; min-width: min(500px, 90vw); max-width: 95vw;">
    <h2 class="text-center w-100">Offer a Ride</h2>
    <form action="/API/add_offer" method="post">
        <div>
            <label>Pickup Location</label>
            <input type="text" name="pickup_location" placeholder="e.g. KC Airport" required>
        </div>
        <div>
            <label>Dropoff Location</label>
            <input type="text" name="dropoff_location" placeholder="e.g. The Joe" required>
        </div>
        <div style="display: flex; gap: 10px;">
            <div style="flex: 1;">
                <label>Pickup Time</label>
                <input type="datetime-local" name="pickup_time" required>
            </div>
            <div style="flex: 1;">
                <label>Dropoff Time</label>
                <input type="datetime-local" name="dropoff_time" required>
            </div>
        </div>
        <div>
            <label>Fee ($)</label>
            <input type="number" name="price" min="0" placeholder="20" required>
        </div>
        <div>
            <label>Available Seats</label>
            <input type="number" name="total_seats" min="1" placeholder="1" required>
        </div>
        <div>
            <label>Notes</label>
            <textarea name="notes" placeholder="Use this (optionally) to clarify any details, like that you're willing to negotiate price for multiple passengers, or that you have a lot of trunk space"></textarea>
        </div>
        <div style="text-align: center; margin-top: 10px;">
            <input type="submit" value="Post Offer" class="btn w-100">
        </div>
    </form>
</div>